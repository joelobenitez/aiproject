# Claude Code / agentes — puede contener tokens o config local
.claude/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/

# Secretos / entorno local
.env
.env.*
!.env.example

# Mosquitto: hash bcrypt, pero fuera de git por prudencia (mismo criterio que .env, D20/T019)
mosquitto/passwd

# Datos de runtime (SQLite del MVP, D9)
data/

# SO
.DS_Store
Thumbs.db

# Spec Kit — cache local si la genera (no se conocen artefactos de este tipo aun)
